# -----------------------------------------------------------------------------
# IMPORT PACKAGES
# -----------------------------------------------------------------------------

import pandas as pd
from ydata_profiling import ProfileReport
# , config

# # Activate the PhiK correlation coefficient
# config.Settings().correlations.phik.compute = True
# config.Settings().correlations.phik.date_correction = True

# # Deactivate other correlations coefficients
# config.Settings().correlations.pearson.compute = False
# config.Settings().correlations.spearman.compute = False
# config.Settings().correlations.kendall.compute = False
# config.Settings().correlations.cramers.compute = False